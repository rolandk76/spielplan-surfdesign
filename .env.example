# Example environment variables for Forge deployment
# Copy to .env and adjust values on the server

# Database
DB_HOST=127.0.0.1
DB_PORT=3306
DB_DATABASE=spielplan
DB_USERNAME=forge
DB_PASSWORD=secret
